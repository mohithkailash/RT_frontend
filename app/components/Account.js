import React from "react";
import { View,Text } from "react-native";

const Account = ({}) => {
    return(
    <View>
        <Text>Find Account details here</Text>
    </View>
    )
}

export default Account;