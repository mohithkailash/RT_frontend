import React from "react";
import { View,Text } from "react-native";

const Makepost = ({}) => {
    return(
    <View>
        <Text>Make a post here</Text>
    </View>
    )
}

export default Makepost;